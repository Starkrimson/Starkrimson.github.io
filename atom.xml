<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Starkrimson&#39; Blog</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://starkrimson.github.io/"/>
  <updated>2018-01-10T04:31:47.157Z</updated>
  <id>https://starkrimson.github.io/</id>
  
  <author>
    <name>Starkrimson</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>简代码大作用</title>
    <link href="https://starkrimson.github.io/2018/01/10/PithyCodes/"/>
    <id>https://starkrimson.github.io/2018/01/10/PithyCodes/</id>
    <published>2018-01-10T04:17:00.000Z</published>
    <updated>2018-01-10T04:31:47.157Z</updated>
    
    <content type="html"><![CDATA[<p>简洁却高效的代码收集。<br><a id="more"></a></p><h3 id="更改-UIButton-ImageView-尺寸"><a href="#更改-UIButton-ImageView-尺寸" class="headerlink" title="更改 UIButton ImageView 尺寸"></a>更改 UIButton ImageView 尺寸</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">button.imageEdgeInsets = <span class="type">UIEdgeInsetsMake</span>(<span class="number">10</span>, <span class="number">0</span>, <span class="number">10</span>, <span class="number">0</span>)</span><br><span class="line"><span class="comment">//同样还有 titleEdgeInsets contentEdgeInsets</span></span><br></pre></td></tr></table></figure><h3 id="更改-UIButton-highlight-backgroundColor"><a href="#更改-UIButton-highlight-backgroundColor" class="headerlink" title="更改 UIButton highlight backgroundColor"></a>更改 UIButton highlight backgroundColor</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="keyword">var</span> isHighlighted: <span class="type">Bool</span> &#123;</span><br><span class="line">    <span class="keyword">didSet</span> &#123;</span><br><span class="line">        backgroundColor = isHighlighted ? .white : <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="获取数组-index"><a href="#获取数组-index" class="headerlink" title="获取数组 index"></a>获取数组 index</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> person1 = <span class="type">Person</span>(name: <span class="string">"John"</span>)</span><br><span class="line"><span class="keyword">let</span> person2 = <span class="type">Person</span>(name: <span class="string">"Sue"</span>)</span><br><span class="line"><span class="keyword">let</span> person3 = <span class="type">Person</span>(name: <span class="string">"Maria"</span>)</span><br><span class="line"><span class="keyword">let</span> person4 = <span class="type">Person</span>(name: <span class="string">"Loner"</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> people = [person1, person2, person3]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> indexOfPerson1 = people.index&#123;$<span class="number">0</span> === person1&#125; <span class="comment">// 0</span></span><br><span class="line"><span class="keyword">let</span> indexOfPerson2 = people.index&#123;$<span class="number">0</span> === person2&#125; <span class="comment">// 1</span></span><br><span class="line"><span class="keyword">let</span> indexOfPerson3 = people.index&#123;$<span class="number">0</span> === person3&#125; <span class="comment">// 2</span></span><br><span class="line"><span class="keyword">let</span> indexOfPerson4 = people.index&#123;$<span class="number">0</span> === person4&#125; <span class="comment">// nil</span></span><br></pre></td></tr></table></figure><h3 id="隐藏-Navigation-bar-和-toolbar-的分割线"><a href="#隐藏-Navigation-bar-和-toolbar-的分割线" class="headerlink" title="隐藏 Navigation bar 和 toolbar 的分割线"></a>隐藏 Navigation bar 和 toolbar 的分割线</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">toolbar.clipsToBounds = <span class="literal">true</span></span><br><span class="line">navigationController?.navigationBar.clipsToBounds = <span class="literal">true</span></span><br></pre></td></tr></table></figure><blockquote><p>需要注意的是，navigation bar 设置 clipsToBounds 为 true 之后，statusBar 的背景颜色将和 navigation bar 的不一致</p><p>所以可以看出来，navigation bar 的原理是设置超出自己范围的 background color，来达成 navigation bar 和 statusBar 背景一致的。</p></blockquote><h3 id="iPad-TableView-全屏幕（iOS-9-）"><a href="#iPad-TableView-全屏幕（iOS-9-）" class="headerlink" title="iPad TableView 全屏幕（iOS 9+）"></a>iPad TableView 全屏幕（iOS 9+）</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">tableView.cellLayoutMarginsFollowReadableWidth = <span class="literal">false</span></span><br></pre></td></tr></table></figure><h3 id="ASDK-解决-NetworkImageNode-闪烁"><a href="#ASDK-解决-NetworkImageNode-闪烁" class="headerlink" title="ASDK 解决 NetworkImageNode 闪烁"></a>ASDK 解决 NetworkImageNode 闪烁</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">imageNode = <span class="type">ASImageNode</span>(viewBlock: &#123; () -&gt; <span class="type">UIView</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> imgView = <span class="type">UIImageView</span>()</span><br><span class="line">    imgView.pin_updateWithProgress = <span class="literal">true</span> <span class="comment">//需要渐进式编码的jpg图片（progressive JPGs），才能实现图片由模糊到清晰加载。</span></span><br><span class="line">    imgView.pin_setImage(from: <span class="type">URL</span>(string: imagePath))</span><br><span class="line">    <span class="keyword">return</span> imgView</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h3 id="iOS9-隐藏-Status-Bar"><a href="#iOS9-隐藏-Status-Bar" class="headerlink" title="iOS9+ 隐藏 Status Bar"></a>iOS9+ 隐藏 Status Bar</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="keyword">var</span> prefersStatusBarHidden : <span class="type">Bool</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//以下两个方法会触发</span></span><br><span class="line"><span class="comment">//navigationController?.setNavigationBarHidden(&lt;#T##hidden: Bool##Bool#&gt;, animated: &lt;#T##Bool#&gt;)</span></span><br><span class="line"><span class="comment">//setNeedsStatusBarAppearanceUpdate()</span></span><br></pre></td></tr></table></figure><h3 id="更改-Status-Bar-背景颜色"><a href="#更改-Status-Bar-背景颜色" class="headerlink" title="更改 Status Bar 背景颜色"></a>更改 Status Bar 背景颜色</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">guard</span> <span class="keyword">let</span> statusBar = <span class="type">UIApplication</span>.shared.value(forKeyPath: <span class="string">"statusBarWindow.statusBar"</span>) <span class="keyword">as</span>? <span class="type">UIView</span> <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line">statusBar.backgroundColor = kBackgroundColor</span><br></pre></td></tr></table></figure><blockquote><p>// TODO: iOS 11 beta 会出现状态栏闪烁，正式版情况不清</p></blockquote><h3 id="Error-code"><a href="#Error-code" class="headerlink" title="Error code"></a>Error code</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">NSURLErrorTimedOut = -1001</span><br><span class="line">NSURLErrorNetworkConnectionLost = </span><br><span class="line">NSURLErrorNotConnectedToInternet = -1009,</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;简洁却高效的代码收集。&lt;br&gt;
    
    </summary>
    
    
      <category term="Swift" scheme="https://starkrimson.github.io/tags/Swift/"/>
    
  </entry>
  
  <entry>
    <title>ReadOn 1.4 发布！性能优化，提高用户体验。</title>
    <link href="https://starkrimson.github.io/2017/11/02/ReadOnReleaseNote/"/>
    <id>https://starkrimson.github.io/2017/11/02/ReadOnReleaseNote/</id>
    <published>2017-11-02T04:00:00.000Z</published>
    <updated>2018-01-12T08:10:39.082Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://itunes.apple.com/cn/app/id1212717344" target="_blank" rel="noopener">ReadOn</a> 1.4 已发布。</p><a id="more"></a><h3 id="ReadOn-1-4-发布！性能优化，提高用户体验。"><a href="#ReadOn-1-4-发布！性能优化，提高用户体验。" class="headerlink" title="ReadOn 1.4 发布！性能优化，提高用户体验。"></a>ReadOn 1.4 发布！性能优化，提高用户体验。</h3><blockquote><p>2017-11-02</p></blockquote><ul><li><strong>优化界面逻辑，页面滑动更流畅</strong></li></ul><hr><h3 id="ReadOn-1-3-0-发布！更换主题，切换字体"><a href="#ReadOn-1-3-0-发布！更换主题，切换字体" class="headerlink" title="ReadOn 1.3.0 发布！更换主题，切换字体"></a>ReadOn 1.3.0 发布！更换主题，切换字体</h3><blockquote><p>2017-10-28</p></blockquote><ul><li><p><strong>暗色主题</strong></p><p>在设置中可以一键切换主题，未来将加入更多颜色主题。</p></li></ul><ul><li><p><strong>增强阅读外观选项</strong></p><p>现在阅读外观选项不止能够调整字体大小，还能切换字体，选择主题。</p><p>but，小抱歉上面提到的功能需要内购升级。</p></li><li><p><strong>单元格侧滑手势</strong></p><p>现在向左侧滑单元格，就能呼出<strong>已读</strong>、<strong>收藏</strong>和<strong>分享</strong>等常用菜单按钮。</p></li></ul><ul><li><p><strong>移除所有广告</strong></p><p>App 内所有广告都已移除。</p></li></ul><hr><h3 id="ReadOn-1-2-3-发布！适配-iPhone-X"><a href="#ReadOn-1-2-3-发布！适配-iPhone-X" class="headerlink" title="ReadOn 1.2.3 发布！适配 iPhone X"></a>ReadOn 1.2.3 发布！适配 iPhone X</h3><blockquote><p> 2017-10-17</p></blockquote><ul><li><p><strong>适配 iPhone X</strong></p><p>为即将到来的 iPhone X 做准备。</p></li></ul><ul><li><p><strong>增加设置项</strong></p><p>增加了多个设置项，包括「标记全部已读需要确认」、「隐藏已读的文章」和「启动时更新文章」。</p></li><li><p><strong>修复上个版本「标记已读」无效 bug</strong></p><p>上个版本写错了一个参数，导致「标记已读」失效或者导致 app crash。1.2.3 版本将修复该功能。</p></li></ul><hr><h3 id="ReadOn-1-2-2-发布！统一管理源"><a href="#ReadOn-1-2-2-发布！统一管理源" class="headerlink" title="ReadOn 1.2.2 发布！统一管理源"></a>ReadOn 1.2.2 发布！统一管理源</h3><blockquote><p>2017-10-12</p></blockquote><ul><li><p><strong>管理订阅源</strong></p><p>在「管理」界面，可以查看最近添加或移除的源；统一移动源到其他文件夹或者移除它们。</p></li></ul><ul><li><p><strong>调整阅读字体大小</strong></p><p>在阅读界面，点击<strong>「阅读外观选项」</strong>，可以调整字体大小。该功能需要内购<strong>升级</strong>。</p></li><li><p><strong>去除广告</strong></p><p>同样包含在<strong>升级</strong>选项，升级后可以移除 app 内的广告，获得纯粹的阅读体验。</p></li></ul><h3 id="ReadOn-1-2-1-发布！VoiceOver-下体验优化"><a href="#ReadOn-1-2-1-发布！VoiceOver-下体验优化" class="headerlink" title="ReadOn 1.2.1 发布！VoiceOver 下体验优化"></a>ReadOn 1.2.1 发布！VoiceOver 下体验优化</h3><blockquote><p>2017-09-23</p></blockquote><ul><li><p><strong>VoiceOver 下体验优化</strong></p><p>以前忽视了 VoiceOver，根据 App Stroe 的留言反馈，我体验了下使用 VoiceOver 操作 ReadOn ，确实不是很好。这个版本优化了很多这方面的体验，相信可以尽可能地无障碍使用 ReadOn 阅读。</p></li></ul><ul><li><p><strong>搜索更多的源</strong></p><p>现在可以搜索到更多的源了，帮你发现更多有趣的内容。</p></li><li><p><strong>增加右侧栏</strong></p><p>😄全新的设计，还在摸索中，目前右侧栏用于投放广告。可能会引起一些用户的反感，还是希望大家能支持一下独立开发者。</p></li></ul><hr><h3 id="ReadOn-1-2-发布！新增同步订阅源功能"><a href="#ReadOn-1-2-发布！新增同步订阅源功能" class="headerlink" title="ReadOn 1.2 发布！新增同步订阅源功能"></a>ReadOn 1.2 发布！新增同步订阅源功能</h3><blockquote><p>2017-09-05</p></blockquote><ul><li><p><strong>一个神奇的按钮</strong></p><p>「<strong>设置</strong>」增加了一个按钮「<strong>使用 iCloud 同步</strong>」。开启后可以同步你的订阅源和已收藏的文章到 iCloud 中。在其他同样登录了该 Apple ID 的设备中，也能查看 iCloud 上已同步的内容。</p><p>注：使用该功能的前提是，系统设置中已登录了 Apple ID，并且 iCloud 是可用的。</p></li><li><p><strong>「添加」页优化</strong></p><p>增加搜索源时的一些提示，当网络问题或者没有搜索到结果时都会有些提示。避免原来添加页一片空白的尴尬。</p><p>搜索 api 可能没有收录所有的源，这时手动输入完整的源 url 也可以添加该源。</p></li><li><p><strong>其他内容更新</strong></p><p>同样在「<strong>设置</strong>」里，可以<strong>清理缓存</strong>和开启<strong>自动清理</strong>功能。</p></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://itunes.apple.com/cn/app/id1212717344&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;ReadOn&lt;/a&gt; 1.4 已发布。&lt;/p&gt;
    
    </summary>
    
    
      <category term="ReadOn Release" scheme="https://starkrimson.github.io/tags/ReadOn-Release/"/>
    
  </entry>
  
  <entry>
    <title>了解 VoiceOver，支持 VoiceOver！</title>
    <link href="https://starkrimson.github.io/2017/09/23/2017-09-23VoiceOver/"/>
    <id>https://starkrimson.github.io/2017/09/23/2017-09-23VoiceOver/</id>
    <published>2017-09-23T02:40:00.000Z</published>
    <updated>2017-10-16T13:53:19.564Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/17-09-05VoiceOver/voiceOver1.png" alt=""><br>上个月 <a href="https://itunes.apple.com/cn/app/id1212717344" target="_blank" rel="noopener">ReadOn</a> 在 <a href="https://itunes.apple.com/cn/app/id1212717344" target="_blank" rel="noopener">AppStore</a> 有条特殊的评论。根据这位小伙伴的反馈，ReadOn 在 VoiceOver 下大体是可以操作的，但细节上体验还不够好。说实话这让我很感动又很惭愧，感动是因为苹果的伟大，它让所有人都能无障碍地使用 Apple 的产品。很惭愧自己之前都没有意识到这点，所以为了能让大家都能无障碍地使用 app，我决定要好好了解一下 VoiceOver，并改善 ReadOn 在这方面的体验。</p><a id="more"></a><h3 id="VoiceOver-是什么"><a href="#VoiceOver-是什么" class="headerlink" title="VoiceOver 是什么"></a>VoiceOver 是什么</h3><blockquote><p>VoiceOver 是一个非常杰出的屏幕朗读器，它面向那些存在视力障碍（包括近视、弱视和完全失明）的用户。<strong>有了 VoiceOver，视障者就好像拥有了一双明亮的眼睛</strong>。通过 VoiceOver，视障用户可以和普通用户一样完全无障碍的使用 Apple 所有操作系统中的内置应用以及很多第三方 App。</p></blockquote><p>事实上，此前我对 VoiceOver 的了解很少，只知道它是苹果家的一个辅助功能，可以朗读屏幕之类。所以在尝试开发 VoiceOver 之前，先得知道它能干些什么，和怎么工作的。然后我找到了一些文章：<a href="https://sspai.com/post/40324" target="_blank" rel="noopener">VoiceOver：Apple 给视障者的一双眼</a>，<a href="https://sspai.com/post/40687" target="_blank" rel="noopener">盲人是如何使用 iPhone 的：专访顾伶磊</a>。</p><h3 id="VoiceOver-基本使用"><a href="#VoiceOver-基本使用" class="headerlink" title="VoiceOver 基本使用"></a>VoiceOver 基本使用</h3><p>以下是我收集的 VoiceOver 常用的手势：</p><ul><li><p>单击</p><p>单击屏幕会让 VoiceOver 选择一个元素，然后会读出指到的内容</p></li><li><p>双击</p><p>双击屏幕能激活当前元素对应的操作，例如打开 app，点击一个 button 等。</p></li><li><p>左划／右划</p><p>单指左滑或者右滑可以屏幕上一个元素或者下一个元素。</p></li><li><p>三指划动</p><p>三指划动可以垂直或水平滚动内容</p></li><li><p>双指 Z 手势</p><p>双指在屏幕划一个 Z 字形返回导航层次的上一级</p></li></ul><h3 id="针对-VoiceOver-的优化"><a href="#针对-VoiceOver-的优化" class="headerlink" title="针对 VoiceOver 的优化"></a>针对 VoiceOver 的优化</h3><h4 id="设置-accessibilityLabel-描述一个控件，让-VoiceOver-朗读出来"><a href="#设置-accessibilityLabel-描述一个控件，让-VoiceOver-朗读出来" class="headerlink" title="设置 accessibilityLabel 描述一个控件，让 VoiceOver 朗读出来"></a>设置 <code>accessibilityLabel</code> 描述一个控件，让 VoiceOver 朗读出来</h4><p>默认情况下，当 VoiceOver 访问到一个 <code>UIButton</code>，它会朗读出该按钮的 <strong>title</strong>，如果这个按钮是纯图片的，它会朗读这张图片的文件名，那么如果是一个空白的按钮，就要自己描述这个按钮了。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">leftMenuBtn.accessibilityLabel = <span class="string">"左侧栏"</span></span><br></pre></td></tr></table></figure><p>然后这里是可以 <strong>localized</strong> 的，如果你也有做国际化，这是不可少的。</p><p>同样地，如果是个 <code>UILabel</code> 则会朗读出 label 的 text。</p><h4 id="更好地描述内容"><a href="#更好地描述内容" class="headerlink" title="更好地描述内容"></a>更好地描述内容</h4><p>ReadOn 有一个 cell，上面显示了文章的标题，发布时间和作者。其中发布时间和作者像是这样的：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">9月 15，周五 | 小明</span><br></pre></td></tr></table></figure><p>这里我想表达的意思是，文章发布于 9月15日，作者是小明。但是 VoiceOver 读出来是这样的「9月15，周五 垂直线 小明」。显然「垂直线」不是我想要的结果。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">.accessibilityLabel = <span class="string">"9月 15，周五，作者：小明"</span> <span class="comment">//这里的结果当然不是固定的字符串，只是方便演示</span></span><br></pre></td></tr></table></figure><p>这样，VoiceOver 就会按照我想要的朗读出来，并且在视觉上并没有改变。</p><h4 id="设置-isAccessibilityElement-控制某个控件是否被VoiceOver-访问"><a href="#设置-isAccessibilityElement-控制某个控件是否被VoiceOver-访问" class="headerlink" title="设置 isAccessibilityElement 控制某个控件是否被VoiceOver 访问"></a>设置 <code>isAccessibilityElement</code> 控制某个控件是否被VoiceOver 访问</h4><p><code>isAccessibilityElement</code> 可以控制某个 <code>UIKit</code> 控件是否可以被 VoiceOver 访问，并且 <code>UIView</code> 默认是 <strong>false</strong> 的。</p><p>在 ReadOn 中，我有个 view，view 里面有个 label。这时 VoiceOver  选中时默认是访问到 view 内的 label。这里我希望能访问到 View，并且读出文件夹名就好。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 首先将 label 设置为不是 Accessibility 的元素</span></span><br><span class="line"><span class="keyword">self</span>.label.isAccessibilityElement = <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 然后设置 view 为 Accessibility 的元素</span></span><br><span class="line"><span class="keyword">self</span>.isAccessibilityElement = <span class="literal">true</span></span><br><span class="line"><span class="keyword">self</span>.accessibilityLabel = text</span><br></pre></td></tr></table></figure><h4 id="「Z」手势返回上一层级。"><a href="#「Z」手势返回上一层级。" class="headerlink" title="「Z」手势返回上一层级。"></a>「Z」手势返回上一层级。</h4><p>默认情况，使用了 navigation 的话，双指 Z 是可以返回上一页。由于 ReadOn 隐藏了 navigation bar，所以这个手势失效了。「Z」手势会触发这个方法 <code>accessibilityPerformEscape()</code>，需要在这里添加上自己想要的返回操作：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">accessibilityPerformEscape</span><span class="params">()</span></span> -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">    navigationController?.popViewController(animated: <span class="literal">true</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="判断-VoiceOver-是否正在使用"><a href="#判断-VoiceOver-是否正在使用" class="headerlink" title="判断 VoiceOver 是否正在使用"></a>判断 VoiceOver 是否正在使用</h4><p><code>UIAccessibilityIsVoiceOverRunning()</code> 可以判断 VoiceOver 是否正在使用。</p><p>可以通过该方法判断 VoiceOver 是否在使用，并做一些改动：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="type">UIAccessibilityIsVoiceOverRunning</span>() &#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;img src=&quot;/images/17-09-05VoiceOver/voiceOver1.png&quot; alt=&quot;&quot;&gt;&lt;br&gt;上个月 &lt;a href=&quot;https://itunes.apple.com/cn/app/id1212717344&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;ReadOn&lt;/a&gt; 在 &lt;a href=&quot;https://itunes.apple.com/cn/app/id1212717344&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;AppStore&lt;/a&gt; 有条特殊的评论。根据这位小伙伴的反馈，ReadOn 在 VoiceOver 下大体是可以操作的，但细节上体验还不够好。说实话这让我很感动又很惭愧，感动是因为苹果的伟大，它让所有人都能无障碍地使用 Apple 的产品。很惭愧自己之前都没有意识到这点，所以为了能让大家都能无障碍地使用 app，我决定要好好了解一下 VoiceOver，并改善 ReadOn 在这方面的体验。&lt;/p&gt;
    
    </summary>
    
    
      <category term="ReadOn" scheme="https://starkrimson.github.io/tags/ReadOn/"/>
    
      <category term="VoiceOver" scheme="https://starkrimson.github.io/tags/VoiceOver/"/>
    
      <category term="Swift" scheme="https://starkrimson.github.io/tags/Swift/"/>
    
  </entry>
  
  <entry>
    <title>ReadOn - 一个 Rss 阅读器</title>
    <link href="https://starkrimson.github.io/2017/09/06/ReadOnIntroduction/"/>
    <id>https://starkrimson.github.io/2017/09/06/ReadOnIntroduction/</id>
    <published>2017-09-06T09:40:00.000Z</published>
    <updated>2017-09-06T08:25:02.000Z</updated>
    
    <content type="html"><![CDATA[<p>开发 ReadOn 也有一段时间了，感觉是时候写一篇文章来介绍一下它了。</p><p>首先关于 ReadOn，它是一款 Rss 阅读器。在这里你可以搜索到喜欢的源并且订阅它，收藏和分享一些有趣的文章。目前可以在 <a href="https://itunes.apple.com/us/app/readon-%E4%B8%80%E4%B8%AA-rss-%E9%98%85%E8%AF%BB%E5%99%A8/id1212717344?l=zh&amp;ls=1&amp;mt=8" target="_blank" rel="noopener">App Store</a> 下载。</p><a id="more"></a><h3 id="设计"><a href="#设计" class="headerlink" title="设计"></a>设计</h3><p>ReadOn 的外观非常简洁。作为一款阅读 app，我首先想到的是将内容占据整个屏幕。所以在一开始设计的时候我就决定不显示顶部的导航栏和底部 tab bar，但是如果没有导航栏，要怎么做到例如「返回」、「选项」等功能呢？手势，ReadOn 使用了很多手势，像右滑返回、长按菜单、3D Touch 预览等。</p><h3 id="主界面"><a href="#主界面" class="headerlink" title="主界面"></a>主界面</h3><p><img src="/images/ReadOnIntroduction/main.png" alt=""></p><p>刚安装上 ReadOn，由于还没有添加订阅源，首页显得有些空荡荡…侧滑划出左侧栏，添加上自己喜欢的源后，主界面饱满许多了。订阅源将根据文件夹分组，左侧显示订阅源的名称，右侧显示未读数，可通过设置关闭未读数的显示。</p><h3 id="订阅"><a href="#订阅" class="headerlink" title="订阅"></a>订阅</h3><p><img src="/images/ReadOnIntroduction/add.png" alt=""></p><p>在「添加」页面。这里集成了一些热门源标签，点击对应的标签即可查看相关的源。或者输入关键字来查找自己想要的源。点击搜索结果，将展示该订阅源的相关信息以及近期发布的文章。右上角的「添加」／「已添加」按钮可以订阅或者不再订阅该源。已订阅的源将会在主界面展示。</p><h3 id="阅读"><a href="#阅读" class="headerlink" title="阅读"></a>阅读</h3><p><img src="/images/ReadOnIntroduction/reading.png" alt=""></p><p>在文章列表中，每行展示该篇文章的标题、作者等信息，如果有缩略图也会一并显示。点击文章即可阅读，如前文所说的，文章内容将占据整个屏幕，可以更专注于阅读。底部有一些常用按钮，例如返回、收藏、分享等，在滑动阅读过程中会隐藏。</p><h3 id="收藏、同步、反馈"><a href="#收藏、同步、反馈" class="headerlink" title="收藏、同步、反馈"></a>收藏、同步、反馈</h3><p><img src="/images/ReadOnIntroduction/other.png" alt=""></p><p>当阅读到喜欢的文章，可以收藏它。收藏的文章统一收录在「收藏」中。可以从左侧栏找到它。</p><p>在 1.2 版本中，我添加了 iCloud 同步订阅源的功能。如果你同时有几台设备，并且希望每台设备都能看到统一的订阅源。那么这个功能非常好，在 iPhone 订阅一个源后，打开 iPad 就能阅读到这个源的文章了。</p><p>如果你喜欢😀（或者不喜欢😔）这款 app，欢迎到 App Store 给我评分。也可以通过邮件告诉我你的建议或者意见。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;开发 ReadOn 也有一段时间了，感觉是时候写一篇文章来介绍一下它了。&lt;/p&gt;
&lt;p&gt;首先关于 ReadOn，它是一款 Rss 阅读器。在这里你可以搜索到喜欢的源并且订阅它，收藏和分享一些有趣的文章。目前可以在 &lt;a href=&quot;https://itunes.apple.com/us/app/readon-%E4%B8%80%E4%B8%AA-rss-%E9%98%85%E8%AF%BB%E5%99%A8/id1212717344?l=zh&amp;amp;ls=1&amp;amp;mt=8&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;App Store&lt;/a&gt; 下载。&lt;/p&gt;
    
    </summary>
    
    
      <category term="ReadOn" scheme="https://starkrimson.github.io/tags/ReadOn/"/>
    
  </entry>
  
  <entry>
    <title>TodayExtension (Widget)</title>
    <link href="https://starkrimson.github.io/2017/04/03/TodayExtension-(Widget)/"/>
    <id>https://starkrimson.github.io/2017/04/03/TodayExtension-(Widget)/</id>
    <published>2017-04-03T02:40:00.000Z</published>
    <updated>2017-09-04T06:51:13.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文将介绍如何在「今日试图」中添加一个扩展，主要以 iOS10 Widget 为主。首先扩展是附属在一个应用内的，也就是我们不能通过 AppStore 直接下载一个扩展，它一定是和宿主 app 一起安装。扩展有自己独立的生命周期。下面将介绍如何创建一个扩展，如何与宿主 app 进行交互。</p><a id="more"></a><p>demo 地址 : <a href="https://github.com/Starkrimson/TodayExtension-Widget-demo" target="_blank" rel="noopener">TodayExtension (Widget)</a></p><h2 id="1-添加-TodayExtendion-target"><a href="#1-添加-TodayExtendion-target" class="headerlink" title="1. 添加 TodayExtendion target"></a>1. 添加 TodayExtendion target</h2><p>点选菜单 <strong>File</strong> &gt; <strong>New</strong> &gt; <strong>Target…</strong>，在 <strong>iOS</strong> &gt; <strong>Application Extension</strong> 可以找到 <strong>TodayExtension</strong>，命名确认后让 Xcode 自动生成新的 Scheme。这时项目中会有一个和刚刚新建的 target 的同名文件夹，里面包含了<code>TodayViewController.swift</code> <code>MainInterface.storyboard</code> <code>info.plist</code> 3个文件。可以看到，在<code>storyboard</code>里默认添加了一个「Hello Wrold」的 label。现在运行一下项目，一切正常的话，会看到如下图：</p><p><img src="/images/TodayExtension/today1.png" alt=""></p><p>关于 widget 内容的展示，在 ViewController 里控制就行，具体想要展示的样子，开发者可以自由完成，这里就不展示了。</p><h2 id="2-iOS10-Widget-高度调整"><a href="#2-iOS10-Widget-高度调整" class="headerlink" title="2. iOS10 Widget 高度调整"></a>2. iOS10 Widget 高度调整</h2><p>如上图，一个 widget 的高度默认是 110。</p><ul><li>在右上角显示「展开/折叠」按钮</li></ul><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 需判断系统版本为10.0以上</span></span><br><span class="line"><span class="keyword">if</span> #available(iOSApplicationExtension <span class="number">10.0</span>, *) &#123;</span><br><span class="line">    extensionContext?.widgetLargestAvailableDisplayMode = .expanded</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// Fallback on earlier versions</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/images/TodayExtension/today2.png" alt=""></p><ul><li>点击「展开/折叠」按钮的回调并控制高度</li></ul><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 同样需要判断版本为10.0以上</span></span><br><span class="line"><span class="meta">@available</span>(iOSApplicationExtension <span class="number">10.0</span>, *)</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">widgetActiveDisplayModeDidChange</span><span class="params">(<span class="number">_</span> activeDisplayMode: NCWidgetDisplayMode, withMaximumSize maxSize: CGSize)</span></span> &#123;</span><br><span class="line">    <span class="keyword">switch</span> activeDisplayMode &#123;</span><br><span class="line">    <span class="keyword">case</span> .compact:</span><br><span class="line">        preferredContentSize = <span class="type">CGSize</span>(width: <span class="number">0</span>, height: <span class="number">110</span>)</span><br><span class="line">    <span class="keyword">case</span> .expanded:</span><br><span class="line">        preferredContentSize = <span class="type">CGSize</span>(width: <span class="number">0</span>, height: <span class="number">220</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/images/TodayExtension/today3.png" alt=""></p><h2 id="3-使用-App-Groups-共享扩展和主应用数据"><a href="#3-使用-App-Groups-共享扩展和主应用数据" class="headerlink" title="3. 使用 App Groups 共享扩展和主应用数据"></a>3. 使用 App Groups 共享扩展和主应用数据</h2><p>由于沙盒的限制，我们不能直接在扩展和主应用间共享数据，这时候要用到 <strong>App Groups</strong>。App Groups 为同一个 vender 的应用或者扩展定义了一组域，在这个域中同一个 group 可以共享一些资源。</p><ul><li>开启 App Groups </li></ul><p>选择主 target 的 <strong>Capabilities</strong> 选项，找到 App Groups 选项，后面有个开关开启。并添加一个 goups name。这里添加一个<code>group.HelloTodayUserDefault</code> 。然后同样的在扩展的 target 也进行同样的步骤。<br><img src="/images/TodayExtension/today4.png" alt=""></p><ul><li>使用同一 groups 下的 userDefault 进行数据共享</li></ul><p>为了方便演示，我在主 target 里添加了一个 textView。textView 的文本修改后，将通过 userDefault 将数据共享给扩展。看下代码：</p><p>在宿主 app 中更改了 textView 的值，同步到 group 的 userDefault 中</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">textViewDidChange</span><span class="params">(<span class="number">_</span> textView: UITextView)</span></span> &#123;</span><br><span class="line"><span class="comment">// suiteName 与 Capabilities 添加的一致</span></span><br><span class="line">    <span class="keyword">let</span> userDefault = <span class="type">UserDefaults</span>(suiteName: <span class="string">"group.HelloTodayUserDefault"</span>)</span><br><span class="line">    userDefault?.<span class="keyword">set</span>(textView.text, forKey: <span class="string">"textView.value"</span>)</span><br><span class="line">    userDefault?.synchronize()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在扩展中获取数据</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> userDefault = <span class="type">UserDefaults</span>(suiteName: <span class="string">"group.HelloTodayUserDefault"</span>)</span><br><span class="line">label.text = userDefault?.value(forKey: <span class="string">"textView.value"</span>) <span class="keyword">as</span>? <span class="type">String</span> ?? <span class="string">"sth. wrong"</span></span><br></pre></td></tr></table></figure><p><img src="/images/TodayExtension/today5.png" alt=""></p><h2 id="4-扩展和主应用共享代码"><a href="#4-扩展和主应用共享代码" class="headerlink" title="4. 扩展和主应用共享代码"></a>4. 扩展和主应用共享代码</h2><p>一个最简单的方式是把需要公用的代码文件加入扩展 target 的编译文件中，但是这么做不好的地方是，当这些共用的文件越来越多，添加到 target 这种方式将难以管理和维护。iOS8 之后 apple 提供了一个更好的方式，做成 Framework。</p><p>点选 <strong>File</strong> &gt; <strong>New</strong> &gt; <strong>Target</strong>…，在 <strong>Framework &amp; Library</strong> 可以找到 <strong>Cocoa Touch Framework</strong>，next 命名。</p><p>上面的代码里，suiteName 我都是直接使用字符串，这里很不好的就是万一两个地方写的不一样了，就有问题了。接下来我在 taget 里添加一个 swift 文件，用一个常量来记录这两个 key 值。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> groupSuiteName = <span class="string">"group.HelloTodayUserDefault"</span></span><br><span class="line"><span class="keyword">let</span> kTextValue = <span class="string">"textView.value"</span></span><br></pre></td></tr></table></figure><p>首先在主程序的 ViewController 中 import 刚刚新建的 Framework。这时我将原来的字符串替换为常量，发现 Xcode 会提示未定义之类的。这是由于同一个 module 中默认 <code>internal</code> 访问层级，现在代码处于不同的 module。所以需要更改 swift 文件的权限，在需要在外部访问的地方加上 <code>public</code> 关键字</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">let</span> groupSuiteName = <span class="string">"group.HelloTodayUserDefault"</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">let</span> kTextValue = <span class="string">"textView.value"</span></span><br></pre></td></tr></table></figure><p>接下来在 ViewController 中就可以直接使用了：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> userDefault = <span class="type">UserDefaults</span>(suiteName: groupSuiteName)</span><br><span class="line">userDefault?.<span class="keyword">set</span>(textView.text, forKey: kTextValue)</span><br><span class="line">userDefault?.synchronize()</span><br></pre></td></tr></table></figure><p>同样的步骤，将 Framework 链接到扩展中</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> userDefault = <span class="type">UserDefaults</span>(suiteName: groupSuiteName)</span><br><span class="line">label.text = userDefault?.value(forKey: kTextValue) <span class="keyword">as</span>? <span class="type">String</span> ?? <span class="string">"sth. wrong"</span></span><br></pre></td></tr></table></figure><p>接下来编译可以通过，但是会收到一条警告：<br><img src="/images/TodayExtension/today6.png" alt=""></p><p>这是由于作为插件，需要严格遵守沙盒限制，一些 API 是不能使用的。避免这个警告，需要在 Framework 中声明使用的是扩展可用的 API。切换到 framework target 中的 <strong>General</strong> 选项，在 <strong>Deployment Info</strong> 下面勾选 <strong>Allow app extension API only</strong>。关于不可用的 API，apple 都标注了 <code>NS_EXTENSION_UNAVAILABLE</code>。</p><h2 id="5-从扩展进入主程序"><a href="#5-从扩展进入主程序" class="headerlink" title="5. 从扩展进入主程序"></a>5. 从扩展进入主程序</h2><p>扩展提供了 <code>NSExtensionContext</code> 类来与主应用交互，这里使用 <code>open</code> 方法就好了。</p><p>这里为了方便演示，我就直接给扩展的 view 添加一个点击手势了。由于这是一个 ViewController，我相信开发者会有很多操作的空间。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">viewDidLoad</span><span class="params">()</span></span> &#123;</span><br><span class="line">...</span><br><span class="line">    <span class="keyword">let</span> tap = <span class="type">UITapGestureRecognizer</span>(target: <span class="keyword">self</span>, action: #selector(tapAction))</span><br><span class="line">    view.addGestureRecognizer(tap)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">tapAction</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> url = <span class="type">URL</span>(string: <span class="string">"todayDemo://helloToday"</span>) <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    extensionContext?.<span class="keyword">open</span>(url, completionHandler: <span class="literal">nil</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在主程序中添加对应的 <strong>URL Scheme</strong>：<br><img src="/images/TodayExtension/today7.png" alt=""></p><p>在 <code>AppDelegate.swift</code> 中捕获打开事件，做出对应操作：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">application</span><span class="params">(<span class="number">_</span> app: UIApplication, <span class="keyword">open</span> url: URL, options: [UIApplicationOpenURLOptionsKey : Any] = [:])</span></span> -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> url.scheme == <span class="string">"todayDemo"</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(url.host) <span class="comment">// -- helloToday</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>End.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文将介绍如何在「今日试图」中添加一个扩展，主要以 iOS10 Widget 为主。首先扩展是附属在一个应用内的，也就是我们不能通过 AppStore 直接下载一个扩展，它一定是和宿主 app 一起安装。扩展有自己独立的生命周期。下面将介绍如何创建一个扩展，如何与宿主 app 进行交互。&lt;/p&gt;
    
    </summary>
    
    
      <category term="Swift" scheme="https://starkrimson.github.io/tags/Swift/"/>
    
      <category term="TodayExtension" scheme="https://starkrimson.github.io/tags/TodayExtension/"/>
    
      <category term="widget" scheme="https://starkrimson.github.io/tags/widget/"/>
    
  </entry>
  
  <entry>
    <title>3D Touch</title>
    <link href="https://starkrimson.github.io/2017/02/08/3D-Touch/"/>
    <id>https://starkrimson.github.io/2017/02/08/3D-Touch/</id>
    <published>2017-02-08T09:40:00.000Z</published>
    <updated>2017-09-04T06:47:02.000Z</updated>
    
    <content type="html"><![CDATA[<p>3D Touch 是 iPhone 6s 及以后机型带来的新功能，是一种立体触控技术，可以感应不同的感压力度。在 app 开发中，主要分为三大模块：<code>Quick Actions</code>、<code>Peek and Pop</code>、<code>Pressure Sensitivity</code></p><a id="more"></a><blockquote><p>iPhone 6s and iPhone 6s Plus bring a powerful new dimension to the Multi-Touch interface with 3D Touch. This new technology senses how deeply users press the display, letting them do more than ever with your apps and games.</p><p>— developer.apple.com</p></blockquote><p>demo 地址：<a href="https://github.com/Starkrimson/3D-Touch-Demo" target="_blank" rel="noopener">3D-Touch-Demo</a></p><h2 id="1-Quick-Actions"><a href="#1-Quick-Actions" class="headerlink" title="1. Quick Actions"></a>1. Quick Actions</h2><p>在 Home 界面按压 app 图片，可以显示最多 4 个标签。点击不同的标签可以进入 app 并且处理相应的事件。下面来看下实现方法</p><p>创建 <code>Quick Actions</code> 有两种方式，1) 在 info.plist 文件中静态声明；2) 在程序初始化时利用代码动态添加</p><ul><li>静态声明<br><img src="/images/3D-Touch/quick-actions-02.png" alt=""><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;key&gt;UIApplicationShortcutItems&lt;/key&gt;</span><br><span class="line">&lt;array&gt;</span><br><span class="line">&lt;dict&gt;</span><br><span class="line">&lt;key&gt;UIApplicationShortcutItemType&lt;/key&gt;</span><br><span class="line">&lt;string&gt;share&lt;/string&gt;</span><br><span class="line">&lt;key&gt;UIApplicationShortcutItemTitle&lt;/key&gt;</span><br><span class="line">&lt;string&gt;分享&lt;/string&gt;</span><br><span class="line">&lt;key&gt;UIApplicationShortcutItemIconType&lt;/key&gt;</span><br><span class="line">&lt;string&gt;UIApplicationShortcutIconTypeShare&lt;/string&gt;</span><br><span class="line">&lt;/dict&gt;</span><br><span class="line">&lt;/array&gt;</span><br></pre></td></tr></table></figure></li></ul><p>如上，其中<code>UIApplicationShortcutItemType</code>、<code>UIApplicationShortcutItemTitle</code>是必填项。<code>UIApplicationShortcutItemIconType</code>、<code>UIApplicationShortcutItemIconFile</code>、<code>UIApplicationShortcutItemSubtitle</code>为选填项。</p><ul><li>动态添加</li></ul><p>通过代码，指定<code>application.shortcutItems</code>快捷方式数组，可在如下方法中添加 (具体位置可根据自己情况而定)。</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">application</span><span class="params">(<span class="number">_</span> application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplicationLaunchOptionsKey: Any]?)</span></span> -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> shareItem = <span class="type">UIApplicationShortcutItem</span>(type: <span class="string">"share"</span>, localizedTitle: <span class="string">"分享"</span>, localizedSubtitle: <span class="literal">nil</span>, icon: <span class="type">UIApplicationShortcutIcon</span>(type: .share), userInfo: <span class="literal">nil</span>)</span><br><span class="line">    <span class="comment">// ... 更多 item 设置</span></span><br><span class="line">    application.shortcutItems = [shareItem]</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行程序后，按压 app 图标，将可以看到一个「分享」的标签。</p><ul><li>响应回调</li></ul><p><code>UIApplication</code>提供了一个方法接听 <code>shortcutItem</code>的点击</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">application</span><span class="params">(<span class="number">_</span> application: UIApplication, performActionFor shortcutItem: UIApplicationShortcutItem, completionHandler: @escaping <span class="params">(Bool)</span></span></span> -&gt; <span class="type">Void</span>) &#123;</span><br><span class="line"><span class="comment">// 可通过之前设置的 type 唯一标示区分点击的按钮，作出不同的响应</span></span><br><span class="line">    <span class="keyword">switch</span> shortcutItem.type &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">"share"</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"clicked share"</span>)</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 iOS10 中，如果该 app 还添加了 todayExtension。在按压 app 图标后，在 quick actions 上方还会显示该 app 的 widget。如果自带的「天气」app。</p><p><img src="/images/3D-Touch/quick-actions-03.png" alt=""></p><h2 id="2-Peek-and-Pop"><a href="#2-Peek-and-Pop" class="headerlink" title="2. Peek and Pop"></a>2. Peek and Pop</h2><p><img src="/images/3D-Touch/peek-and-pop-01.png" alt=""></p><p>Peek and Pop 大概分为 3 个步骤，1) 轻按，焦点高亮其余部分变模糊。2) 加大力度，出现目标 ViewController 预览。3) 这时可以继续重压 push 至目标 ViewController (或者自定义动作)；或者手指上滑，将出现右图的<code>PreviewAction</code></p><p>由于 3D Touch 需要 iPhone 6s 以上机型支持，所以需要判断 3D Touch 的可用性</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">traitCollection.forceTouchCapability == .available ? <span class="number">_</span> = registerForPreviewing(with: <span class="keyword">self</span>, sourceView: cell) : ()</span><br></pre></td></tr></table></figure><p>这里使用了一个三目，如果是可用的，则执行<code>registerForPreviewing</code>注册，设置 cell 为 sourceView</p><p>接下来遵循<code>UIViewControllerPreviewingDelegate</code>协议，实现其方法</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">ViewController</span>: <span class="title">UIViewControllerPreviewingDelegate</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 重压后触发</span></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">previewingContext</span><span class="params">(<span class="number">_</span> previewingContext: UIViewControllerPreviewing, commit viewControllerToCommit: UIViewController)</span></span> &#123;</span><br><span class="line">        navigationController?.pushViewController(<span class="type">DetailViewController</span>(), animated: <span class="literal">true</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 返回触发 Peek 事件的原始图</span></span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">previewingContext</span><span class="params">(<span class="number">_</span> previewingContext: UIViewControllerPreviewing, viewControllerForLocation location: CGPoint)</span></span> -&gt; <span class="type">UIViewController</span>? &#123;</span><br><span class="line">        <span class="keyword">let</span> detailViewController = <span class="type">DetailViewController</span>()</span><br><span class="line">        <span class="keyword">return</span> detailViewController</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上滑后出现的 <code>PreviewAction</code>需要在目标 ViewController 中实现。同样需要注册 <code>registerForPreviewing</code>和遵循<code>UIViewControllerPreviewingDelegate</code>。并且额外重写一个计算型属性</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="keyword">var</span> previewActionItems: [<span class="type">UIPreviewActionItem</span>] &#123;</span><br><span class="line">    <span class="keyword">let</span> item = <span class="type">UIPreviewAction</span>(title: <span class="string">"Greet"</span>, style: .<span class="keyword">default</span>) &#123; (action, viewController) <span class="keyword">in</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"Hello"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> [item]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;3D Touch 是 iPhone 6s 及以后机型带来的新功能，是一种立体触控技术，可以感应不同的感压力度。在 app 开发中，主要分为三大模块：&lt;code&gt;Quick Actions&lt;/code&gt;、&lt;code&gt;Peek and Pop&lt;/code&gt;、&lt;code&gt;Pressure Sensitivity&lt;/code&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="Swift" scheme="https://starkrimson.github.io/tags/Swift/"/>
    
      <category term="3D Touch" scheme="https://starkrimson.github.io/tags/3D-Touch/"/>
    
  </entry>
  
  <entry>
    <title>如何愉快地处理 Optional</title>
    <link href="https://starkrimson.github.io/2016/10/29/About-Swift-Optional/"/>
    <id>https://starkrimson.github.io/2016/10/29/About-Swift-Optional/</id>
    <published>2016-10-29T00:54:00.000Z</published>
    <updated>2017-09-03T10:34:22.000Z</updated>
    
    <content type="html"><![CDATA[<p>比较好的几种处理 Optional 的方法。</p><a id="more"></a><h2 id="1-「-」-操作符"><a href="#1-「-」-操作符" class="headerlink" title="1.「 ?? 」 操作符"></a>1.「 ?? 」 操作符</h2><p>在使用 <code>??</code> 之前，先来复习下三目运算符：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="built_in">max</span> = a &gt; b ? a : b</span><br><span class="line"><span class="comment">//如果 a &gt; b 为真，返回 a， 为假返回 b。</span></span><br></pre></td></tr></table></figure><p>再来看下 <code>??</code> 的用法</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> i: <span class="type">Int</span>? = <span class="number">100</span></span><br><span class="line"><span class="keyword">let</span> x = i ?? <span class="number">0</span> <span class="comment">// x = 100</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> y: <span class="type">Int</span>? = <span class="literal">nil</span></span><br><span class="line"><span class="keyword">let</span> z = y ?? <span class="number">0</span> <span class="comment">// y = 0</span></span><br></pre></td></tr></table></figure><p><code>??</code> 与三目运算类似，如果一个 optional 有值，使用值。如果没值，使用 <code>??</code> 后面的值。</p><p>如果按住 <code>⌥ Optoian</code> 点击 <code>x</code> 和 <code>z</code>。可以看到它们的类型推导为 <code>let x: Int</code> <code>let z: Int</code>，并<strong>不是</strong>一个可选值</p><p>利用 <code>??</code> 操作符，可以起到给一个 optional 属性设置一个默认值的作用。 在后续使用中不需要考虑解包的问题。</p><h2 id="2-Optional-Binding-可选绑定"><a href="#2-Optional-Binding-可选绑定" class="headerlink" title="2. Optional Binding 可选绑定"></a>2. Optional Binding 可选绑定</h2><h3 id="2-1-if-let"><a href="#2-1-if-let" class="headerlink" title="2.1 if let"></a>2.1 <code>if let</code></h3><p>来看一个小🌰：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">contact</span><span class="params">(name: String?, phone: String?)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> name = name, <span class="keyword">let</span> phone = phone &#123;</span><br><span class="line">    <span class="comment">// 只有当 name 和 phone 一定有值，</span></span><br><span class="line">    才会进入该分支</span><br><span class="line">        <span class="keyword">let</span> contact = name + <span class="string">" "</span> + phone <span class="comment">// 可以直接参与计算</span></span><br><span class="line">        <span class="built_in">print</span>(contact)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"name or phone nil"</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>例子中的函数，有两个参数并且都是可选的。那么在函数内部就要考虑到参数的解包问题。使用 if let 可选绑定成功后，name 和 age 都一定有值，可直接使用。它们的类型推导都是非可选值。</p><p>可以看到，我在使用 <code>if let name = name</code> 的时候，使用了同名的变量来接收值，在后续使用都是非空值。可以避免想变量名的烦恼…</p><p>在整个函数实现内部，没有出现一个 <code>?</code> 或者 <code>!</code> 。是不是看到和清爽？(并且更安全)</p><h3 id="2-2-guard-let"><a href="#2-2-guard-let" class="headerlink" title="2.2 guard let"></a>2.2 <code>guard let</code></h3><p>guard let 刚好与 if let 相反</p><p>再来一个🌰：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">contact</span><span class="params">(name: String?, phone: String?)</span></span> &#123;</span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> name = name, <span class="keyword">let</span> phone = phone <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 当 name 或者 phone 为 nil，才会进入该分支</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">"name or phone nil"</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="comment">// 在分支的最后，需要有 return 或者 break ...</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果没有进入 guard 分支，则 name 和 phone 都有值，可直接参与计算</span></span><br><span class="line">    <span class="keyword">let</span> contact = name + <span class="string">" "</span> + phone</span><br><span class="line">    <span class="built_in">print</span>(contact)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>guard let 在日常开发使用的更多一些，我们可以在 guard 分支里做一些错误处理，然后返回。</p><p>如果没有进入 guard 分支，那么后面那些变量的都是有值的。可以直接使用</p><blockquote><p>同样的还有 <code>if var</code> 、 <code>guard var</code></p></blockquote><h2 id="3-Optional-Map"><a href="#3-Optional-Map" class="headerlink" title="3. Optional Map"></a>3. Optional Map</h2><p>如果有  <code>let i: Int?</code> ，i 是一个整型可选值，我想对它 *2 并且返回一个新的值。如果 i 有值的话就 *2 然后返回，如果没值则返回 nil。使用可选绑定可以这么写：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x: <span class="type">Int</span>?</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">let</span> i = i &#123;</span><br><span class="line">    x = i * <span class="number">2</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    x = <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但有个更简洁的方法是使用 <code>map</code></p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> i: <span class="type">Int</span>? = <span class="number">100</span></span><br><span class="line"><span class="keyword">let</span> x = i.<span class="built_in">map</span> &#123; $<span class="number">0</span> * <span class="number">2</span> &#125; <span class="comment">// --&gt; let x: Int?; x = 200</span></span><br></pre></td></tr></table></figure><p>使用 <code>map</code> 可以方便地操作一个 optional，而不需要进行手动的解包工作</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;比较好的几种处理 Optional 的方法。&lt;/p&gt;
    
    </summary>
    
    
      <category term="Swift" scheme="https://starkrimson.github.io/tags/Swift/"/>
    
      <category term="Optional" scheme="https://starkrimson.github.io/tags/Optional/"/>
    
  </entry>
  
  <entry>
    <title>Xcode8 的 3 个小技巧</title>
    <link href="https://starkrimson.github.io/2016/10/22/Xcode8-three-tips/"/>
    <id>https://starkrimson.github.io/2016/10/22/Xcode8-three-tips/</id>
    <published>2016-10-22T08:59:53.000Z</published>
    <updated>2017-09-04T06:57:56.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>Xcode8</code> 更新好多新特性，其中就增加了好多以前需要安装插件才能实现的功能。如下文要说到的 <code>Color Literal</code> <code>Image Literal</code> <code>Documentation</code> 都能找到对应的插件。只能说 <code>Xcode</code> 抢饭碗实力一流！！！</p><a id="more"></a><h2 id="Color-Literal"><a href="#Color-Literal" class="headerlink" title="Color Literal"></a>Color Literal</h2><p>一般写颜色大概是这样的：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> color = <span class="type">UIColor</span>(red: <span class="number">100</span>/<span class="number">255.0</span>, green: <span class="number">150</span>/<span class="number">255.0</span>, blue: <span class="number">200</span>/<span class="number">255.0</span>, alpha: <span class="number">1</span>)</span><br></pre></td></tr></table></figure><p>但是如果不 <code>⌘ + R</code> ，我不知道这颜色到底长什么样。接下来使用 <code>Color Literal</code> 看看</p><p>如下图，我要给 <code>view</code> 设置一个背景颜色，输入 <strong>color literal</strong> 会有智能提示，回车。</p><p> <img src="/images/Xcode8-three-tips/color1.png" alt=""></p><p>然后会弹出一个颜色选择的界面：</p><p><img src="/images/Xcode8-three-tips/color2.png" alt=""></p><p>可以选择一个想要的颜色，还可以点击下方的 <code>Other</code> ，选颜色蜡笔或者直接输入颜色值。选完后在代码里就会有个小方块。里面填充的就是刚刚选的颜色。</p><p>如果把那句代码拷贝出来，其实是这样的：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">view.backgroundColor = #colorLiteral(red: <span class="number">1.0</span>, green: <span class="number">1.0</span>, blue: <span class="number">1.0</span>, alpha: <span class="number">1.0</span>)</span><br></pre></td></tr></table></figure><p>回到最上面那句颜色代码，用上 <code>Color Literal</code> 后，在编译器里就能直接看到颜色了。</p><p><img src="/images/Xcode8-three-tips/color3.png" alt=""></p><h2 id="Image-Literal"><a href="#Image-Literal" class="headerlink" title="Image Literal"></a>Image Literal</h2><p>如图，我在资源库有一张名为 <strong>defaultAvatar</strong> 的图片：</p><p><img src="/images/Xcode8-three-tips/image1.png" alt=""><br>传统的写法大概是这样的：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> img = <span class="type">UIImage</span>(named: <span class="string">"defaultAvatar"</span>)</span><br></pre></td></tr></table></figure><p>与颜色很像，我要 <code>⌘ + R</code> 才能看见是不是想要的图片。而且如果我把 key 写错，图片就不会显示了。</p><p>使用 <code>Image Literal</code> 有两种方式。第一种和颜色一样，输入 <strong>image literal</strong> 出现智能提示，回车</p><p><img src="/images/Xcode8-three-tips/image2.png" alt=""></p><p>出现一个图片选择器，上面有图片的缩略图。</p><p><img src="/images/Xcode8-three-tips/image3.png" alt=""></p><p>第二种是直接输入图片名字，也会有智能提示和缩略图</p><p><img src="/images/Xcode8-three-tips/image4.png" alt=""></p><p>两种方式写好图片后，会像上图一样。左边有个小方块，填充了图片缩略图，右边是图片名</p><p>然后代码拷贝出来是这样子的：</p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> img = #imageLiteral(resourceName: <span class="string">"defaultAvatar"</span>)</span><br></pre></td></tr></table></figure><p>参考：<a href="http://help.apple.com/xcode/mac/8.0/#/dev4c60242fc" target="_blank" rel="noopener">Xcode Help-Add a literal</a></p><h2 id="Add-Documentation"><a href="#Add-Documentation" class="headerlink" title="Add Documentation"></a>Add Documentation</h2><p>接下来要分享的是添加注释文档的快捷键： <code>⌘ command</code> + <code>⌥ option</code> + <code>/</code></p><p>这个快捷键的功能跟 <code>VVDocumenter</code> 一样，可以给函数名，变量名等生成好看的注释文档</p><p><img src="/images/Xcode8-three-tips/documentation1.png" alt=""></p><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// 求和</span></span><br><span class="line"><span class="comment">///</span></span><br><span class="line"><span class="comment">/// - parameter x: x</span></span><br><span class="line"><span class="comment">/// - parameter y: y</span></span><br><span class="line"><span class="comment">///</span></span><br><span class="line"><span class="comment">/// - returns: 和</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">sum</span><span class="params">(x: Int, y: Int)</span></span> -&gt; <span class="type">Int</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> x + y</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在调用函数的时候，按下 <code>⌥ option</code> 和左键，就能看到该函数的注释信息<br><img src="/images/Xcode8-three-tips/documentation2.png" alt=""></p><blockquote><p>噢对了，上面提到到两个 <code>Literal</code> 并不支持 <code>OC</code> (反正我是敲不出来了) 。注释文档这个快捷键都支持，<code>Swift</code> 和 <code>OC</code> 都可以用</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;code&gt;Xcode8&lt;/code&gt; 更新好多新特性，其中就增加了好多以前需要安装插件才能实现的功能。如下文要说到的 &lt;code&gt;Color Literal&lt;/code&gt; &lt;code&gt;Image Literal&lt;/code&gt; &lt;code&gt;Documentation&lt;/code&gt; 都能找到对应的插件。只能说 &lt;code&gt;Xcode&lt;/code&gt; 抢饭碗实力一流！！！&lt;/p&gt;
    
    </summary>
    
    
      <category term="Xcode8" scheme="https://starkrimson.github.io/tags/Xcode8/"/>
    
  </entry>
  
</feed>
